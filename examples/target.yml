# This is rims
spec:
  deploy:
    containers:
      nginx:
        image: nginx:latest
      go-inline:
        image: 1.26.4 # vt:cimg/go:full
      go-split:
        image: cimg/go
        tag: 1.26.4 # vt:cimg/go:tag
